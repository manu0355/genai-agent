i want to build an agent, which should contains these components :
1. a local llm connected using ollama
2. tools 
2.1 api tool,
2.2 terminal access tools,
2.3 code execution tool,
2.4 folder access tool
3. a permenent memory to capture important facts accoss conversations
3.1 a temporary memory to capture current conversation.
